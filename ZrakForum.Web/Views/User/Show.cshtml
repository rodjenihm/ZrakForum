@model ZrakForum.Web.Model.UserShowViewModel

@{
    ViewData["Title"] = Model.Username;
}

<h1>Prikaz korisnika</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Username)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Username)
        </dd>
    </dl>
</div>
